import 'package:flutter/material.dart';

class AppColors {
  // General
  static const seedColor = Colors.green;
  static const transparent = Color(0x00FFFFFF);
  static const white = Color(0xFFFFFFFF);
  static const selectionWhiteBackground = Color(0x50FFFFFF);
  static const selectionBlackBackground = Color(0x20000000);

  // Background
  static const backgroundGradientStart = Color(0xFFE4E4EE);
  static const backgroundGradientEnd = Color(0xFFC2C2CE);

  // Buttons
  static const buttonPrimary = Color(0xFFEAEAEA);
  static const buttonShadowColor = Color(0x20202020);

  // Text
  static const header = Color(0xFF202020);
  static const paragraph = Color(0xFF505050);
  static const secondary = Color(0xFF707070);

  // Cards
  static const cardOuterBackground = Color(0x70FFFFFF);

  // Icons
  static const iconColor = Color(0xFF202020);
}
